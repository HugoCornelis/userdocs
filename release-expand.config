#!/usr/bin/perl -w

my $config
    = {
       files => [
		 './Makefile.am',
		 './bin/userdocs-version',
		 './configure.ac',
		 './tests.config',
		 './tests/specifications/version.t',
		 './tests/tests.config',
# 		 './VERSION',
		],
       labels => {
		  email => '',
		  label => '',
		  major => '',
		  micro => '',
		  minor => '',
		  monotone_id => `mtn automate get_current_revision_id`,
		  package => '',
		 },
      };

return $config;

